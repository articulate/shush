version: '2'
services:
  redis:
    image: redis:latest
    expose:
      - "6379"

  app:
    build: .
    depends_on:
      - redis
    labels:
      - "SERVICE_9393_NAME=shush"
      - "SERVICE_9393_TAGS=load-balance"
