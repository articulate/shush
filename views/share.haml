%h2 Here's your YURL

%input.form-control{ type: "text", value: url, onclick: "this.focus();this.select();" }

- if time
  %p= "This link will expire in #{time}."
- else
  %p= "This link will expire when read."
